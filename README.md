# 🎵 Glazbeni Player - [NAZIV PROJEKTA]

Ovaj projekt predstavlja funkcionalni, jednostavan glazbeni player izgrađen isključivo korištenjem **HTML5**, **CSS3** i **Vanilla JavaScripta**.

Fokusiran je na čiste i učinkovite kontrole reprodukcije i prilagođen je strukturi `index.html` koju ste poslali.

---

## ✨ Ključne Funkcionalnosti

Player podržava sljedeće osnovne funkcije:

* **Upravljanje Reprodukcijom:**
    * Gumbi za Reprodukciju (`▶️`), Pauzu (`⏸️`), Stop (`⏹️`).
    * Navigacija na Prethodnu (`⏮️`) i Sljedeću (`⏭️`) pjesmu.
* **Prikaz Statusa:** Prikaz trenutnog vremena (`#current-time`) i ukupnog trajanja (`#duration`) pjesme.
* **Kontrola Napretka:** Interaktivna klizna traka (`#progress`) za brzi skok na željeni dio pjesme.
* **Popis Pjesama:** Jednostavni popis (`#playlist`) za odabir i navigaciju između dostupnih pjesama.

---

## 🛠️ Tehnologije

| Tehnologija | Uloga u Projektu |
| :--- | :--- |
| **HTML5** | Definira osnovnu strukturu i audio element (`<audio>`). |
| **CSS3** | Stilizacija i vizualni izgled playera (datoteka `style.css`). |
| **JavaScript (ES6+)** | Logika reprodukcije, obrada klikova na gumbe i ažuriranje statusa vremena (datoteka `script.js`). |

---

## 🚀 Instalacija i Pokretanje

Nema potrebe za kompleksnom instalacijom. Player je dizajniran za lokalno pokretanje.

### 1. Struktura Datoteka

Obavezno je da se glazbene datoteke nalaze u mapi `Muzika/`, točno kako je definirano u HTML kodu:

![muzički player](muzički player.png)